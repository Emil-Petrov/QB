import React from 'react';

const options = { 
    itemType: {
        label: 'List Items',
        type: 'form',
    },
}

export { options };

const typeListComplex = () => (<div>List Complex</div>);

export default typeListComplex;