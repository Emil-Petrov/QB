import React from 'react';

const options = { 
    fromDate: {
        label: 'From',
        type: 'number',
    },
    toDate: {
        label: 'To',
        type: 'number',
    }
}

export { options };

const typeDateTime = () => (<div>DateTime</div>);

export default typeDateTime;